// store connection information and URL shortender

var config = {};

config.db = {};

config.webhost = "http://localhost:5000/";

//mongoDB host and database name
config.db.host = "localhost";
config.db.name = "url_shortender";

module.exports = config;
